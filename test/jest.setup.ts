// This is a jest setup file to import `reflect-metadata` to supply the `tsyringe` library.
// We can also add some prerequisites that are required to run the automated test cases.

import 'reflect-metadata';
import * as dotenv from 'dotenv';
dotenv.config();
